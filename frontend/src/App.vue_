fileName: App.vue
---
<template>
    <v-app>
        <v-container>
            <component
                    :is="getComponentName"
            ></component>
        </v-container>
    </v-app>
</template>

<script>
    export default {
        name: "App",
        data: () => ({
            routes: [
            {{#aggregates}}
                { 
                    path: '/{{boundedContext.namePlural}}/{{namePlural}}',
                    componentName: '{{boundedContext.namePascalCase}}{{namePascalCase}}Manager',
                },
                {
                    path: '/{{boundedContext.namePlural}}/{{namePlural}}/:id',
                    componentName: '{{boundedContext.namePascalCase}}{{namePascalCase}}Detail',
                },
            {{/aggregates}}
            {{#views}}
                {
                    path: '/{{namePlural}}',
                    componentName: '{{namePascalCase}}View',
                },
                {
                    path: '/{{namePlural}}/:id',
                    componentName: '{{namePascalCase}}ViewDetail',
                },
            {{/views}}
            ],
        }),
        methods: {
            getComponentName() {
                var componentName = "";
                this.routes.forEach(item => {
                    if (item.path === location.href) {
                        name = item.componentName;
                    }
                });
                return componentName;
            }
        },
    };
</script>

<style>
</style>
